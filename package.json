{
	"name": "puppylog",
	"module": "app.ts",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json && bun build ./ts/app.ts --outfile=./assets/puppylog.js",
		"test": "bun test",
		"format": "prettier --write --no-semi ."
	},
	"devDependencies": {
		"@types/bun": "^1.2.17",
		"bun-types": "^1.2.0"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"prettier": "^3.5.3"
	}
}
